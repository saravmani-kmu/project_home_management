[build]
  base    = "todo-app"
  command = "npm install && npx ng build --configuration production"
  publish = "dist/todo-app/browser"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
